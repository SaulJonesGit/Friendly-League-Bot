# Getting Started

Run `npm run start` to start the bot

Run `ngrok http 4008` to create the port forward strategy

Run `npm run register` to update the bot with newly added commands

# The .env

Various values will need to be in the local .env
 
Copy the .env.example and update with the various api keys which can be obtained from discord and riot dev sites